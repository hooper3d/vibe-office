# Project

项目名：AG-UI 推广网页开发

定位：Project Context Hub MVP 的安全示例项目。

当前目标：

- 保持单页面、轻量控制台，不做完整后台。
- 跑通前端通过 AG-UI 发送意图、后端以 SSE 返回事件流、前端展示事件和生成结果。
- 用 Markdown 文件模拟共享记忆，让 Ray / Lucy / Tiger 复用同一份项目事实。

验收重点：

- 页面不再出现旧的交易类测试项目。
- 快捷动作能触发 AG-UI 事件流。
- Ray 动作能更新 Project Context Hub。
- Tiger 能基于 BLOG_CONTEXT / RELEASE_NOTES 写 Blog。
